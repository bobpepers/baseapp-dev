.cr-search {
  margin: var(--gap);
  padding: var(--gap);
  width: calc(100% - var(--gap) * 5);

  &__icon {
    padding: var(--gap);

    img {
      height: 17px;
      width: 17px;
    }
  }

  &__input {
    border: none;
    border-bottom: 1px solid var(--text-primary);
    color: var(--text-primary);
    padding: var(--gap);

    &::placeholder {
      color: var(--text-primary);
    }

    &:focus {
      outline: none;
    }
  }
}
