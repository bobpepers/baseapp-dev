.modal-body__withdraw-submit {
    text-align: center;
}

.modal-footer__withdraw-submit {
    input {
        border-radius: 4px;
        font-weight: normal;
        background: var(--button-normal);
        color: var(--bright);
        font-size: calc(var(--gap) * 2.66);
        height: calc(var(--gap) * 5);
        margin: 0;
        text-align: center;
        cursor: pointer;

        &:hover {
            background: var(--button-hover);
        }
    }
}
